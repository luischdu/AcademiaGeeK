function ProcesoPesado(iteracion) {
  for (let index = 0; index < iteracion; index++) {
    console.log("Hola clase");
  }
  return `El numero de iteraciones fue ${iteracion}`;
}

export default ProcesoPesado;
